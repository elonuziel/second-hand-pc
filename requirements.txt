requests>=2.31.0
urllib3>=2.0.0
curl_cffi>=0.7.0

# Optional: headless-browser fallback for JavaScript bot challenges (see README).
# Not installed by default because it also needs a ~120MB Chromium download.
# playwright>=1.40

